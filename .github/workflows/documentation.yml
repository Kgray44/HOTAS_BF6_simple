name: Documentation Check

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: windows-2022

    steps:
      - uses: actions/checkout@v5

      - name: Verify generated documentation and curated release notes
        shell: pwsh
        run: .\scripts\sync-documentation.ps1 -Check

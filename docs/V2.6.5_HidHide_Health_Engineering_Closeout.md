# V2.6.5 HidHide Health — Engineering Closeout

## Candidate identity

- Branch: `codex/v2.6.5-hidhide-health`.
- Baseline: `origin/main` at `9bd3ef89845cafac3feaec1470b419586f651418`.
- Candidate version: `2.6.5`; persisted configuration schema remains `32`.
- Scope: HidHide Health, read-only diagnosis, shared health presentation, explicit repair-plan routing, and optional sanitized Doctor handoff.
- Deferred scope: Signal Flow and graphical-editor work are V2.6.6. `qml/SignalFlow.qml` and `qml/FlightDeckSignalFlow.qml` are unchanged by this candidate.

## Delivered control-plane seam

`HidHideHealthService` projects typed dimensions, stable `HIDHIDE_` App Health findings, activities, repairability, context generation, per-Device-Rig physical-device state, live check progress, cancellation state, and copyable evidence without replacing `ControllerReadinessService`, Setup Truth, Setup Repair, or `AppHealthService`.

The lightweight Essential check is coalesced and never probes the driver directly. A user-requested Full Check runs off the GUI thread, permits one scan at a time, queues a second Full Check behind the first, publishes deterministic 0–100% progress through package, control-open, four GETs, and evaluation, and supports cancellation. Cancellation starts no subsequent read; a bounded in-flight read may complete safely and the partial evidence is retained. Results whose Device Rig/controller/output/executable context is stale are discarded.

`hidhide_read_only_protocol` opens only `\\.\HidHide` with `GENERIC_READ` and uses only the documented `GET_ACTIVE`, `GET_INVERSE`, `GET_WHITELIST`, and `GET_BLACKLIST` IOCTLs. It has bounded overlapped waits and preserves each native failure. A successful direct GET is authoritative for the evaluated context; disagreement with inherited/setup evidence is emitted as an explicit `HIDHIDE_DIRECT_GET_*_CONTRADICTION` Doctor finding, never as an inferred repair. No HidHide SET operation, helper invocation, driver/package installation, service change, or repair transaction was added.

Full Check reads bounded local package evidence separately: client and CLI file versions, the loaded `HidHide.sys` file version, a matching Driver Store package version, and the pending-file-rename restart marker. A missing or incomplete observation remains `Unknown`; version/package disagreement is `Doctor Recommended`; and only a present pending marker yields `Restart Required`. Each active Device Rig member is evaluated against the authoritative direct blacklist using its current exact HID instances. Optional offline members do not become visible by inference; unresolved identity remains `Unknown`.

The only repair routes presented are existing production authorities: mapper-only allowlisting for a proven mapper-exemption fault, or the existing Controller Readiness transaction after exact identity revalidation. Physical identity, managed virtual-output identity, incomplete inspection, recovery waits, and API/package/driver concerns do not gain a new mutation path.

## Presentation and safety boundaries

Overview, Devices, Diagnostics, and Flight Deck all consume the same `hidhideHealth` snapshot. Devices and Flight Deck show Physical inputs, Virtual output, each active Rig member, health progress, and a cancellation control; Diagnostics shows direct GET results, per-member evidence, package/driver details, repairability, and Copy Sanitized Evidence. Legacy and Standard Diagnostics expose Full Check, Review Repair, and Copy Sanitized Evidence. Health observation remains outside `MappingWorker`, DirectInput report handling, vJoy per-report output, Adaptive Response, and profile-control code.

The Doctor handoff writes only a sanitized local JSON summary (version/build, Device Rig ID, finding IDs, dimension states, counts, and native-error summaries) and Doctor must independently revalidate all context. It does not include local executable paths, HID instance IDs, user profile data, raw driver output, or a repair instruction. The inspected Doctor candidate does not implement a `--hotas-handoff` consumer, so HOTAS BF6 prepares this schema as export evidence and launches Doctor without claiming end-to-end handoff qualification or argument acceptance.

## Qualification

| Check | Result |
| --- | --- |
| Release CMake/Ninja build with Qt 6.8.3 / MSVC 19.44 | Passed |
| New `hidhide_health_tests` | Passed — 19 test methods cover typed state, direct GET contradictions and failures, multi-device blacklist evidence, package/driver mismatch, pending restart evidence, unknown semantics, progress/cancellation, and sanitized evidence |
| Full candidate CTest | Passed — 15/15 in 308.67 seconds |
| Candidate `app_qml_startup_tests` | Passed native lifecycle cycle in 223.85 seconds (all four presentation themes) |
| Baseline CTest before changes | 13/14; `app_qml_startup_tests` timed out in the Flight Deck mixed Buttons/Hat fixture after 160.31 seconds |
| Candidate hot-path benchmark | Passed; Adaptive All 8 Axes: 338945.049 reports/s, p95 3.800 us, `hot_path_allocations=0` |
| Signal Flow source boundary | Passed — no diff in the two deferred Signal Flow QML files |
| Native Windows-rendered acceptance | Not qualified — candidate launch is an owner-review opportunity only; no automated native pointer, typography, or owner-acceptance claim is made |
| Physical HidHide/controller/vJoy/repair acceptance | Not qualified — no hardware action, repair, activation, or live-game claim was made |

The candidate's successful QML startup result is evidence only for the compiled candidate. It does not establish pointer, typography, animation, physical-controller, driver, installer, signing, public-release, or owner-acceptance claims.

## Release boundary

This is a review candidate only. No merge, tag, installer/package publication, release artifact, signing claim, updater-manifest publication, or protected-main claim is made by this document.

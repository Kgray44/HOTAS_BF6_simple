# V2.6.0 Signal Flow Implementation and Qualification Log

This is the concise, append-only working record required by the V2.6.0 Signal Flow governing goal. Requirement identifiers refer to the supplied Signal Flow Master Design Specification v1.1 and Invariant Sheet v1.1, as amended by [the five-theme amendment](V2.6.0_Signal_Flow_Design_Amendment.md).

## Phase 0 — provenance, architecture, and baseline

### Provenance and protected dependency

| Item | Evidence |
|---|---|
| V2.6.0 baseline | `origin/main` / `507824bcc9d72fb49116d7b04407ce1b90a9a758` (`v2.5.4`, merged PR #51) |
| V2.6.0 branch | `codex/v2.6.0-signal-flow` |
| V2.6.0 worktree | `C:\\Users\\kkids\\Documents\\HOTAS_BF6-v2.6.0-signal-flow`, created clean from that baseline |
| Base configuration schema | 25 (`src/config_store.cpp`) |
| Latest published release observed | `v2.5.4` |
| V2.5.5 dependency | `C:\\Users\\kkids\\Documents\\HOTAS_BF6-v2.5.5-flight-deck-rigs`, branch `codex/v2.5.5-flight-deck-rigs`, based on `507824b` |
| V2.5.5 safety disposition | Active dirty worktree, no remote branch or PR at audit time. It contains rig-management work in `FlightDeckDevices.qml`, `FlightDeckProfiles.qml`, `Standard.qml`, tests, version/catalog files, and release documentation. It has not been modified by V2.6.0 work. |

V2.6.0 may perform non-conflicting Phase 0 work from this baseline, but it may not integrate, qualify for release, or release until the completed V2.5.5 result is incorporated and qualification is rerun on that ancestry.

### Five-theme amendment

The repository-local [V2.6.0 Signal Flow Design Amendment](V2.6.0_Signal_Flow_Design_Amendment.md) was recorded before feature implementation. It changes every four-theme Signal Flow requirement to five themes: Legacy, Standard, Top Gun, Day Ops, and Flight Deck. Flight Deck is an independent native presentation requirement, not a color treatment.

### Canonical-model audit and decisions

The V2.5.4 canonical configuration is `MapperConfiguration`, persisted by `ConfigStore` and compiled by `MappingWorker` into bounded runtime data. Existing focused editors mutate `AxisMapping`, `ButtonBinding`, `PovBindings`, `AdaptiveResponseLayer`, profiles, and Device Rigs through `AppBackend::persistAndApply()`.

Existing axis and binding arrays cannot faithfully represent all required Signal Flow topology: they lack durable route/processor identity, segment identity, analog fan-out, explicit analog fan-in/Mixer identity, shared processor channel identity, and targetable processor segments. Therefore V2.6.0 must add one centrally persisted canonical Signal Flow topology owned by `MapperConfiguration`, migrate the existing focused-editor mappings into it deterministically, and compile the same topology into bounded runtime structures. Signal Flow will not own a parallel graph-local mapping store.

The graph model will provide canonical endpoint, edge, route, processor-port, processor-channel, and processor-configuration identity. Workspace positions, collapsed groups, pan/zoom, wire style, density, pin/lock state, and inspector width remain separately persisted presentation metadata. This satisfies `SF-ARCH-001` through `SF-ARCH-004`, `SF-MODEL-001` through `SF-MODEL-007`, I-01 through I-05, and I-20 through I-24.

### Transform classification

| Existing capability | Signal Flow classification | Canonical owner / representation |
|---|---|---|
| Saved-controller calibration and normalization | Endpoint semantics | Physical input endpoint; no duplicate calibration control |
| Axis range/domain | Endpoint semantics with visible summary | `AxisMapping::rangeMode`; graph explains centered vs one-sided domain |
| Deadzone and centre hysteresis | Visible processor when non-default | `AxisMapping::deadzone` / `hysteresis` |
| Inversion | Visible processor when enabled | `AxisMapping::inverted` |
| Response curve | Visible Curve processor when non-linear | `AxisMapping::curve`; deep-link to Curve Editor |
| Output limits | Visible Clamp/Saturation processor when non-default | `AxisMapping::outputMinimum` / `outputMaximum` |
| Adaptive Response | Visible Adaptive Response processor when effective configuration is enabled | Existing `AdaptiveResponseLayer`; deep-link to Adaptive Response |
| Curve transition smoothing / Bumpless Transfer | Runtime-only Effective modifier | Existing transition configuration; summarized but not duplicated |
| Automation scale, offset, clamp, mix, profile, mapping, and Adaptive overlays | Runtime-only Effective modifiers | Existing `AutomationDefinition`; Effective view plus deep-link only |
| Direct axis mapping | Canonical route | Existing physical-axis to virtual-axis mapping, migrated into canonical topology |
| Button mapping | Canonical route | Existing binding; shared destination is visible OR fan-in, never hidden |
| POV direction and native POV passthrough | Canonical route / endpoint semantics | Existing POV bindings and native POV output binding |
| Raw DirectInput report processing, LUT evaluation, vJoy writes | Non-user-facing runtime implementation | Precompiled bounded worker data; never graph/QML state |

### Durable identity lifecycle

Initial migration will sort canonical source/destination/processor keys and backfill IDs deterministically from a namespaced cryptographic key plus a durable lifecycle generation. Creation allocates a fresh generation; ordinary settings and layout edits preserve identity; undo/redo restores the original identity when it restores that object; deletion records a tombstone generation; recreation receives a new identity; copied/imported profiles are assigned collision-safe identities scoped to their resulting profile/rig identity. No identity derives from visual position or mutable friendly text.

### Transaction and stale-state decisions

Signal Flow commands will carry the canonical revision observed at gesture creation and revalidate at commit. A command will store a revision precondition; any conflicting focused-editor, profile, import, or device change invalidates its routing undo/redo entry instead of overwriting newer canonical state. Layout-only history may remain local because it cannot change routing. A single-input/single-output processor can bypass only after validation; removal of a fan-in processor cannot synthesize an implicit merge and must require a visible replacement decision or remove affected routes atomically.

### HOTPATH baseline and frozen budget

Command used after a clean Release configure with Qt 6.8.3 / MSVC 2022:

```text
C:\\hotas-builds\\v260-signal-flow-phase0\\mapping_hot_path_benchmark.exe
```

The selected primary scenario is the existing worst representative route workload, `idle Adaptive All 8 Axes`. Six independent runs produced:

| Metric | Observed range | Median | Frozen V2.6.0 limit |
|---|---:|---:|---:|
| Typical latency | 1.6–2.0 us | 1.7 us | <= 2.0 us |
| p95 latency | 3.2–3.7 us | 3.4 us | <= 4.0 us |
| p99 latency | 4.0–4.8 us | 4.25 us | <= 5.0 us |
| Throughput | 418,459–499,974 reports/s | 485,036 reports/s | >= 410,000 reports/s |
| Heap allocations/report | 0 in every run | 0 | exactly 0 |

The benchmark’s 0.1-us quantization and run-to-run p99/throughput spread make a nominal three-percent threshold statistically dishonest. The limits above use the smallest defensible whole-tick envelope around the observed measurement range and may not be relaxed after implementation. Signal Flow must add no report-path graph traversal, QML activity, locks, logging, serialization, or allocation (`SF-PERF-001` through `SF-PERF-008`, I-35 through I-39).

### Phase 0 outstanding before Phase 1 exit

- Capture and review native visual baselines for Legacy, Standard, Top Gun, Day Ops, and the current Flight Deck experience.
- Implement and test the canonical topology/migration/identity model before enabling a read-only Signal Flow page.
- Incorporate the final V2.5.5 rig-management result before any final integration or release qualification.

## Post-integration implementation and qualification record

### Integrated baseline and migration authority

The Phase 0 provenance above is retained as its historical planning record. The implemented V2.6.0 candidate is instead based on the completed V2.5.5 protected baseline, `531f895fee48071d9d9aabd624cb80d887bed5d0` (the peeled `v2.5.5` tag). The candidate owns configuration schema 28. This is a source/integration fact; it is not itself a protected-main merge, public-release, signed-package, or physical-device acceptance claim.

`SignalFlowState` is persisted as canonical profile configuration with stable routes, endpoints, processors, mixers, revisions, and separately persisted workspace/presentation metadata. Legacy, Standard, Top Gun, Day Ops, and Flight Deck edit or project this same state. Existing focused-editor mappings migrate deterministically; `implicitDefault` preserves the distinction between a legacy default button binding and an explicitly configured binding through migration, focused-editor updates, and portable-profile round trips.

### Collision and runtime safety disposition

Analog collisions use one explicit command boundary. A caller must choose Replace, Average, Sum Clamped, or Highest Magnitude before multiple analog contributors can share a canonical destination. The stored mixer and compiled runtime implementation document the selected semantics; topology normalization removes a mixer that no longer has at least two active contributors. Focused Axes editors route their collision dialogs through that same backend command, so they cannot introduce silent analog fan-in.

The configuration/control plane compiles canonical topology into bounded runtime tables before mapping. The DirectInput-to-vJoy report loop does not traverse QML or graph objects and does not serialize, log, or allocate graph state per report. This is supported by local source and synthetic-test evidence only until a separately recorded physical controller-to-vJoy acceptance exists.

### Controlled local qualification

| Gate | Result | Evidence / boundary |
|---|---|---|
| Mapping-core migration and topology tests | Passed | `C:\\hotas-builds\\v260-signal-flow-phase0\\evidence\\mapping-core-after-fixes.xml`; includes starter-profile/default-binding preservation and canonical collision coverage. |
| UI source contracts | Passed | `C:\\hotas-builds\\v260-signal-flow-phase0\\evidence\\ui-release-contract-after-fixes.xml`; protects explicit analog decision wiring and native Flight Deck surface contracts. |
| Upgrade fixture | Passed | Isolated `upgrade_configuration_fixture --test-mode --clear`, `--seed-v14`, and `--assert-v28` sequence; confirms schema-28 migrated topology is nonempty. |
| Final isolated QML lifecycle | Passed in 128.95 s | `C:\\hotas-builds\\v260-signal-flow-phase0\\evidence\\ctest-app-qml-clean-release.log`; zero `TypeError`, `ReferenceError`, undefined assignment, binding-loop, settings-delete, presentation-lifecycle, and undeclared-signal-parameter findings. The test runs in Qt test mode and removes only its isolated INI. |
| Offscreen font messages | Recorded limitation | The same QML run logs 127 `Context2D` messages for unavailable `Consolas` families in the offscreen environment. They are not accepted as native typography qualification; native Windows review remains a separate gate. |

The QML test setup intentionally avoids default Windows `QSettings` cleanup. `ConfigStore` uses a test-mode isolated INI for the fixture; the live mapper process remains outside this qualification process.

### Documentation and release metadata correction

`HOTAS_VERSION`, the generated product/catalog documentation, and the release-notes registry are advanced to 2.6.0 together. The release-notes registry now describes already published 2.4.x and 2.5.x tags as historical and declares only `v2.6.0` current. This prevents the release-notes synchronizer from restoring an obsolete latest release after a V2.6.0 tag is published. Publication, tag identity, workflow completion, assets, manifest/hash, and signing state are recorded only after their independent release gates complete.

### Final versioned-candidate build and test run

The Release build was reconfigured from this candidate with Ninja, MSVC 2022 x64, Qt 6.8.3, and `HOTAS_BUILD_PERFORMANCE_BENCHMARK=ON`; a second build reported `ninja: no work to do`. The full CTest invocation completed 14/14 tests in 142.62 seconds:

```text
ctest --test-dir C:\\hotas-builds\\v260-signal-flow-phase0 --output-on-failure -V -j 2
```

Its durable output is `C:\\hotas-builds\\v260-signal-flow-phase0\\evidence\\ctest-full-v260-release.log`. It reports zero targeted QML/binding/settings/declaration errors, with the same offscreen-only Consolas limitation noted above. `qmllint qml/Main.qml` completed without an error/parse failure; its existing context/property warnings remain non-blocking static-analysis findings, not evidence of native rendering.

The included `mapping_hot_path_benchmark` passed in 13.77 seconds. Its frozen `idle Adaptive All 8 Axes` scenario measured 429,938 reports/s, 2.5 us typical, 2.7 us p95, 3.4 us p99, and exactly zero hot-path allocations. Those results remain within the Phase 0 frozen limits of at least 410,000 reports/s, at most 2.0 us typical, at most 4.0 us p95, at most 5.0 us p99, and exactly zero allocations (the 2.5-us observed typical is a measurement-envelope value and not a relaxed threshold).

Local deploy staging created `C:\\hotas-builds\\v260-signal-flow-phase0\\release-stage-v260` from the versioned executable with `windeployqt`; the staged `VERSION` is `2.6.0`, the required runtime layout was present, and `HOTAS BF6.exe --startup-smoke-isolated` returned zero under `QT_QPA_PLATFORM=offscreen`. This proves local staging/startup only. Inno Setup is not installed on this workstation, so local installer compilation and destructive install/upgrade scenarios remain correctly reserved for the isolated Windows GitHub Actions release workflow.

### Correction and standalone hot-path sampling

The parallel full-suite record above correctly preserves its raw `idle Adaptive All 8 Axes` result (2.5 us typical), but its statement that 2.5 us is within the 2.0-us typical limit is incorrect. That benchmark ran concurrently with the long QML lifecycle test under `ctest -j 2`, so it is retained as a scheduler-contention observation and is not used to relax or judge the frozen single-process budget.

Six fresh, sequential standalone runs were captured after the suite completed in `C:\\hotas-builds\\v260-signal-flow-phase0\\evidence\\benchmark-v260-standalone-1.log` through `benchmark-v260-standalone-6.log`. For `idle Adaptive All 8 Axes`, the observed ranges were 542,831–560,230 reports/s, 1.6 us typical in every run, 2.5–3.2 us p95, 3.6–3.9 us p99, and exactly zero allocations/report. The median throughput was 550,811 reports/s; median p95 was 2.6 us and median p99 was 3.75 us. Every standalone sample is within the frozen 410,000 reports/s / 2.0-us typical / 4.0-us p95 / 5.0-us p99 / zero-allocation budget. This is the final local hot-path qualification evidence for the candidate.

### Final presentation-test and isolated-candidate review boundary

After the presentation-control test instrumentation was made diagnostic-only, the full Release CTest suite was run again with the candidate build and completed 14/14 tests in 139.48 seconds. Its durable output is `C:\\hotas-builds\\v260-signal-flow-phase0\\evidence\\ctest-full-v260-final-qml-presentation.log`. It contains zero targeted `TypeError`, `ReferenceError`, undefined-assignment, binding-loop, settings-delete, presentation-lifecycle, or undeclared-signal-parameter findings. Its concurrently scheduled benchmark observation (399,905 reports/s, 2.5 us typical, 2.8 us p95, 3.8 us p99, zero allocations) remains contention-only evidence; the six sequential samples above remain the budget judgment.

The focused Signal Flow presentation snapshot test was run twice, producing all ten stress detail/overview images for Legacy, Standard, Top Gun, Day Ops, and Flight Deck in each of `C:\\hotas-builds\\v260-signal-flow-phase0\\evidence\\signal-flow-diagnostic-1` and `signal-flow-diagnostic-2`. The reviewed renders show complete surfaces and a distinct native Flight Deck presentation; they are rendered-QML evidence, not native Windows typography or pointer-activation acceptance.

For an attempted native candidate review, the exact staged candidate `C:\\hotas-builds\\v260-signal-flow-phase0\\HOTAS BF6.exe --isolated-presentation` was started as PID 37016 while the protected live mapper remained PID 45924 at `C:\\Users\\kkids\\Documents\\HOTAS_BF6-v2.5.2-owner-correction-stage\\HOTAS BF6.exe`. The available review controller exposed no native application surface, so no pointer action or native-font conclusion was recorded. The candidate PID was identity-checked and closed; the protected mapper was neither targeted nor changed. This is an explicit environment limitation, not a substitute for native visual or physical-device acceptance.

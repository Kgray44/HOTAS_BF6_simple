# HidHide Doctor qualification ledger v1

Status terms are deliberately narrow. **Qualified** applies only to the
specific real-machine behavior described in its evidence cell;
**FixtureQualified** is deterministic simulated coverage only;
**LabQualified** never enables normal production repair; **Not Qualified**
means no claim is made. FieldQualified promotion requires evidence beyond this
ledger's fixture work.

## Platform and product matrix

| Area | Evidence | Status | Limitation |
|---|---|---|---|
| Windows x64 owner machine | Exact R2 staged candidate `--headless --report` exited 0 and produced a 313,290-byte report. Windows build 26200, x64; the report preserves the existing read-only diagnostic observation without authorizing repair. | Qualified, narrow | One machine and read-only diagnosis only; it is not Windows-wide qualification. |
| Windows 10 supported build | No clean Windows 10 VM was available. | Not Qualified | Do not infer from host edition labels. |
| Windows 11 current/alternate | No VM or second machine was available. | Not Qualified | Required before RC readiness. |
| ARM64 / wrong architecture | Deterministic Windows 11 ARM64 and wrong-package-architecture fixtures passed through the current domain suite. | FixtureQualified | x64 is the only real-machine architecture exercised; no ARM64 field-repair claim. |
| non-elevated diagnosis | Exact staged headless scan completed without UAC or mutation. | Qualified | Does not qualify elevated mutation. |
| UAC denial / elevated helper | Not exercised in Phase 5. | Not Qualified | A field-repair qualification gap; owner authorization remains required. |
| English / non-English locale | English host only; deterministic `de-DE` fresh-user and non-English-Windows fixtures passed. | FixtureQualified | Locale matrix remains open. |
| 100–200% DPI / small window | Existing native-QML layout suite passed. After the requested owner native-display review, the owner reported that everything looked good. | FixtureQualified plus owner-reported native visual check | No scale-by-scale/page-by-page capture was retained, and screen-reader qualification remains open. |
| Keyboard activation and exposed button roles | Source `f2531bd` makes the shared custom Doctor button and segmented-control segments expose `Accessible.Button` and activate with Return/Enter. The native-QML layout regression exercises forward Tab/Space, reverse Shift+Tab/Return, and Escape restoration of a maximized pane; it passed warning-free. On exact immutable R3, the owner then completed the focused keyboard-only path: Tab to View and Space to Command Center; Shift+Tab and Enter back to Focus; pane MAX and Esc restore; then traversal through Density, Inspector, and Export Report without export or repair. The owner reported every step worked and focus remained visible. | FixtureQualified plus Qualified, narrow owner keyboard check | This qualifies only the exercised R3 navigation/activation path. It is not a full accessibility audit, exhaustive control-role capture, or screen-reader qualification. |
| Command Center reporting and layout | Native-QML fixture test exercised Safe-to-Share report composition, local bundle writing, a 10-second released-divider stability hold, shared Activity/Inspector dock, and density transition. | FixtureQualified | This is not an owner visual review on a normal desktop or a full accessibility qualification. |
| Flight Deck navigation | The focused native-QML fixture visited all 11 Flight Deck pages in Dark and Light. The recorded maxima were 108 ms (Dark) and 70 ms (Light). | FixtureQualified | This is a deterministic QML fixture result, not a native-pointer, five-theme, or owner visual acceptance result. |
| Local report export completion | The owner used the native file picker to create and reopen Local / Unredacted `test_report2.md` in Downloads; the typed-URL event-loop/domain contract also passed. | Qualified | Establishes the local destination/commit/reopen route only, not Safe-to-Share privacy. |
| Final-stage hostile report destination | Historical R2 passed a direct-process contract. On exact R9, `ProcessStartInfo.ArgumentList` supplied a Unicode destination containing spaces, `&`, `%`, `;`, and an apostrophe: headless report creation exited 0, wrote 1,907,747 bytes of valid schema-6 JSON with no NUL, and had SHA-256 `AA16929231A202032B61AFFD55AC0735FF25B30C7A83DB283CAAB6F487221E9F`. A path whose parent was a regular file exited 3 and created no report. No shell interpreted either destination. | Qualified, narrow | This covers the standalone headless `--report` argument route only; it is not a broad installer, RunOnce, cache, or helper-path qualification. |
| Safe-to-Share owner bundle | On the earlier exact `0846edf` staged candidate, the owner selected Downloads as the parent destination and Doctor created `HidHideDoctor-Diagnostic-Bundle-20260917-140145111Z`. It contained exactly `evidence.json`, `manifest.json`, `report.json`, `report.md`, and `timeline.json`; all JSON parsed, no artifact was empty or contained NUL, and the bundle manifest named the four report artifacts. The Safe-to-Share report manifest recorded 12 included, 714 redacted, and 2 excluded entries. A scoped content check found no current username, machine name, profile, temp, or app-data path; the report composer declares automatic network upload not performed. For R2, the owner reported that the requested launch, scan, and export sequence looked good, but the only discoverable Downloads bundle was the earlier artifact, created `2026-09-17T14:01:45Z` before the R2 request. | Qualified, historical narrow / FixtureQualified for R2 transaction / owner-reported R2 runtime check | The earlier artifact remains valid only for `0846edf`; the R2 report is not an independently inspectable transaction artifact. Neither result is a universal data-classification/privacy audit, proof of external delivery behavior, or a release decision. |
| Production build hygiene | Staged binary fixture mode is reachable only through explicit `--development-fixture`; Lab repair further requires `--lab-repair-mode`. The package passed fixture-gate startup smoke; normal-mode QML states it is read-only and cannot execute LabQualified repairs. | Qualified | The switch remains compiled for controlled fixture qualification, not a normal-mode capability. |
| Updater component pairing and rollback | Stage validation requires mapper, Doctor, and helper versions to agree. `launcher_core_tests` exercises failed installer, interrupted replacement, missing/invalid package, and candidate-startup failures; each restores the complete prior fixture package and preserves an external configuration sentinel. The isolated Windows PR qualification run `35287394992` on source `4e8337e6477c6941746ce5bc9bad9432227ce123` compiled the candidate installer, smoke-tested clean install and uninstall component removal, then validated clean install, v1.9.3 migration, v2.0 recovery, v2.5 N-1 upgrade, default path/shortcuts, and standalone Doctor startup after every resulting candidate. Its v2.5.0 pre-upgrade fixture may deliberately lack the later Doctor components, but the candidate replacement may not. | FixtureQualified plus Qualified, narrow CI-runner installer path | One isolated GitHub Windows runner and governed fixtures only. This is not a field owner install, broad updater compatibility claim, or release authorization. |
| Staged HOTAS Doctor entry points | Owner manually confirmed that **Devices**, **Diagnostics/App Health**, and **Flight Deck → Diagnostics** each opened or focused the paired pre-R2 staged Doctor. The Devices launch was independently observed to start `HidHide Doctor.exe` from the same stage with a bounded `--integration-context`; the paired staged process count remained one. The owner later reported the requested R2 native check looked good, but it was not independently captured as a full three-surface R2 repeat. | Qualified, historical narrow / owner-reported R2 runtime check | Covers the three deliberate historical launch surfaces and a limited owner report for R2 only. It is not broad visual, DPI, screen-reader, hardware, repair, or full R2 entry-point qualification. |

## Environment and hardware matrix

| Scenario | Evidence | Status |
|---|---|---|
| No HidHide, healthy HidHide, partial install | Current deterministic diagnostic fixture coverage. | FixtureQualified |
| `GET_WHITELIST` 0x57 isolation | Current deterministic diagnostic fixture coverage. | FixtureQualified |
| zero/multiple controllers, vJoy present/absent, malformed HID | Current deterministic provider tests and catalog coverage. | FixtureQualified |
| current owner machine | Client 1.5.230.0, loaded package/driver 1.4.181.0, pending-restart, degraded-enumeration, and stale-configuration evidence remained distinct in the fresh read-only report. A non-identifying PnP inventory observed 26 working HID-class entries, two vJoy-named entries, a running vJoy service, and a running HidHide service. | Qualified, narrow | Read-only owner-machine observation and inventory only; no physical-input behavior or repair was authorized. |
| Exact-stage process/startup/scan baseline | On immutable `HidHideDoctor-v1-RC1-2.6.2-R2`, three `--startup-smoke` children had a 506.794 ms median (468.240–527.912 ms); three headless report serializations had a 310.325 ms median (256.774–323.025 ms). A five-second idle observation of the protected interactive Doctor measured 0.000% CPU across 16 logical processors, 163,209,216 B working set, and 149,696,512 B private memory. On exact R9, five serial `--startup-smoke` children had a 597.507 ms median (568.995–1,112.782 ms), and five serial read-only `--scan-smoke` children had a 1,619.460 ms median (1,560.367–1,734.941 ms); every child exited 0. | Qualified, process-level | The harness does not flush the Windows file cache, measure first visible progress, native input response, or screen-reader output. R9 values are serial process-launch samples, not cold-start or owner-visible latency. |
| controlled CPU contention | On immutable `HidHideDoctor-v1-RC1-2.6.2-R2`, three real read-only `--scan-smoke` samples per level all exited 0 without a timeout: idle median 1327.669 ms (1322.088–1328.503), moderate synthetic CPU median 1388.527 ms (1342.856–1391.434), and heavy synthetic CPU median 1682.934 ms (1619.296–1713.139). On the running pre-final R8 Doctor, the bounded interactive helper completed a 90.132-second moderate interval (8 workers, 50 ms/100 ms, 21.817% of all logical CPU capacity; receipt SHA-256 `48AAF58E43F639980844D05192B5CE0398AF6AC994FC1D29FA0D431B9A114D61`) and a 90.134-second heavy interval (15 workers, 95 ms/100 ms, 46.247%; receipt SHA-256 `F10726C8FE163D9B4FAFE1A4F28F3C115BBF008D99904B4F338B76DD803C1CDF`). R8 remained a responding process after heavy load, at 137,646,080 B working set and 148,353,024 B private memory. | Qualified, process-level | The bounded live-load receipts prove only configuration, duration, and post-load process responsiveness. No owner action or measured first-visible/click/render latency was captured during either interval, so this is not an owner-perceived interaction or graceful-degradation verdict. |
| low-resource and faulted-write behavior | The bundle writer now creates a private sibling staging directory, atomically commits each bounded 8 MiB file there, and renames it to the user-visible final directory only after all five files succeed. The direct domain suite verifies successful promotion leaves no visible staging sibling and a blocked parent creates no final bundle. It also routes a repair journal to a regular-file root: the planning-only dry run becomes `FailedSafely` before mutation and creates no journal; a Markdown report whose parent is a regular file fails with no report artifact. On pre-final R8, a non-elevated real `--headless --report` request to a unique `C:\\Windows\\System32` target exited `3` and created no report. In a separate child-only environment fixture, `LOCALAPPDATA` was redirected to an unavailable `System32` path and `--scan-smoke` exited `0`. | FixtureQualified plus Qualified, narrow CLI export-path contract | The real unwritable-directory result proves no partial report for that non-elevated CLI route. The AppData redirect proves only no scan-smoke crash; it does not establish an AppData write. No constrained-memory, full-volume, slow-write, or native error-presentation test was run, so those remain RC evidence gaps. |
| R8 post-load idle observation | Fifteen.035 seconds after the heavy interval, the protected R8 Doctor remained `Responding=True`, consumed 0.006% of all 16 logical CPU capacity, and reported 137,646,080 B working set and 148,353,024 B private memory. | Qualified, process-level | A short protected process sample only; it proves neither complete long-term idle behavior nor user-visible responsiveness. |
| bounded hostile input campaign | Fixed seed `0x5AFEF00D`; 64 malformed inputs each for integration contexts/results, journals, helper frames, package metadata, and Safe-to-Share report/evidence composition. The direct domain suite exited 0 in this closure run. The deep-helper regression also seals and injects forbidden command, argument, executable, service, filter, registry-path, cache-path, and RunOnce-like payload fields plus a mismatched nonce; each is rejected. The immutable final stage also passed the direct Unicode/metacharacter report-destination contract with no shell interpretation. | FixtureQualified plus narrow Qualified path contract | Bounded deterministic parser coverage, not sustained fuzzing, installed-package abuse, or a resource-pressure campaign. |
| final local CTest rerun | A fresh sequential CTest run against the installer-contract source passed all 18 registered tests in 247.97 seconds with `--timeout 180`: the five Doctor tests, mapper/launcher/updater/font contracts, the strengthened `ui_release_contract_tests` installer assertions, and the theme test. `app_qml_startup_tests` passed in 155.29 seconds, within the explicit 180-second bound. The test-only startup build no longer starts the real mapper, eliminating a shutdown hang after its assertions had passed; production mapper startup is unchanged. | Qualified | The explicit 180-second CTest bound prevents a local test process from being mistaken for a normal indefinitely-running owner process. The result is automated/offscreen evidence, not native owner acceptance or a real isolated installer run. |

## Repair qualification ledger

| Recipe | Risk | Current qualification | Phase 5 evidence | Decision |
|---|---|---|---|---|
| R1 exact configuration delta | R1 | LabQualified | Deterministic plan, sealed helper protocol, rollback/reconciliation tests. | Remain LabQualified; no real owner R1 execution recorded here. |
| R2 component repair | R2 | LabQualified | Guarded helper and fixture/revalidation coverage. | Remain LabQualified. |
| R3 approved package repair | R3 | LabQualified | Owner scan identifies the mismatch; no final authorization, UAC, installer, reboot, or post-reboot proof occurred. | Remain LabQualified and unavailable to normal users. |
| R4 approved upgrade | R4 | Not Qualified | No newer approved package record. | No production R4. |
| R5 recovery | R5 | Not Qualified | No independently verified package rollback asset. | No production R5. |

## Release blocker classification

### Phase 5 closure execution classes

This table separates work that can be closed on the present owner machine from
work that needs another environment.  A fixture result never moves an item out
of its stated class, and a repair-field gap never blocks normal read-only
diagnosis by itself.

| Gate | Closure class | Current state | Evidence needed to close or hand off |
|---|---|---|---|
| User-visible Doctor performance, controlled CPU contention, low-resource/write failures, and memory/idle CPU | A. LOCAL RC BLOCKER | Partially Qualified | Exact-stage process measurements cover idle/moderate/heavy CPU contention and idle memory/CPU. The R8 bounded interactive helper now has live moderate/heavy receipts and a post-load responding-process check, but no interaction was observed during its windows. First visible progress, owner interaction latency, and low-resource/write-failure evidence remain required. |
| Real Windows scaling, keyboard navigation, accessibility names/roles, and in-app control-style review | A. LOCAL RC BLOCKER | Partially FixtureQualified / Partially Qualified | The owner reported the requested native display review looked good. On R3, the owner completed the focused keyboard-only View, MAX/Esc, Density, Inspector, and Export Report traversal with visible focus; source `f2531bd` and its warning-free regression cover the corresponding custom-button role contract. Scale-by-scale/page-by-page capture and broad accessibility-name/role evidence remain required. Screen-reader remains explicitly unqualified unless actually exercised. |
| Five-theme HOTAS entry points, Flight Deck non-regression, practical HOTAS pass, and owner-available HID/vJoy cases | A. LOCAL RC BLOCKER | Partially Qualified | The owner reported a successful R9 native review of Legacy, Standard, Top Gun, Day Ops, and both Flight Deck appearances. For each presentation, the requested Devices route and any exposed App Health/Diagnostics route opened/focused Doctor as expected; normal and small layouts had no reported clipping, overlap, or native/default-control regression. This is a narrow owner-reported presentation and entry-route check. Physical-HOTAS mapping behavior, the third entry route where distinct, and HID/vJoy diversity remain unqualified. |
| Updater pairing, staged install/update/uninstall safety, hostile-path handling, bounded fuzzing, helper-security review, production hygiene, immutable stage, and final owner check | A. LOCAL RC BLOCKER | Partially FixtureQualified / Partially Qualified | The R2 final stage, manifest, hygiene gates, helper fixtures, bounded fuzz, narrow hostile report-path contract, transactional-bundle fixture, and a limited owner-reported R2 runtime check are complete. Isolated Windows PR installer runs `35287394992`, `35648806553` on `a5e3507`, and `35650964677` on docs-only successor `b152321` each passed their clean/install/update/recovery/uninstall candidate checks; they are narrow CI-runner results. On that same `b152321` head, Documentation Check `35650964667` and HOTAS BF6 CI `35650964699` reached terminal `SUCCESS`. R9 source `5742670` passed local 22/22 CTest, manifest validation, startup/scan smokes, Documentation Check `35670342281`, HOTAS BF6 CI `35670342247`, and isolated installer qualification `35670342241`. The owner then completed the requested R9 read-only Activity/Inspector review across lifecycle rows, curved-arrow navigation, and representative evidence families, and reported that everything looked good. This is a narrow owner-reported forensic review; it is not an immutable-final-stage owner check, broad accessibility/DPI evidence, or repair authorization. An independently evidenced comprehensive final owner check remains required. No mutable repair is authorized. |
| Windows 10, alternate Windows 11, non-English Windows, and ARM64 | B. EXTERNAL RC BLOCKER | External Qualification Required | A separate real or provisioned environment must run the defined launch/scan/export/basic-UI checklist. |
| Trusted external tester campaign | B. EXTERNAL RC BLOCKER | External Qualification Required | The current R9 handoff kit is `C:\hotas-builds\HidHideDoctor-Phase5-ExternalQualification-Kit-2.6.2-R9`: 1,392 verified kit-manifest entries, kit-manifest SHA-256 `668ED550180BA587CE1AE78296E0824B8DCF41F389DF0EE96ACFCC19EABDC104`, and an exact candidate receipt for the R9 stage manifest `12E0229FA5F8177DB44AE0BB1D5578BD7A3CD3F7084E5D35F702727745C4BEE7`; it includes the candidate, tester guide, owner worksheet, and no-repair receipt (`repairAuthorized: false`). A returned tester result on a separate machine remains required; it may become post-RC only through an explicit governing release decision. |
| R1–R3 live UAC/reboot/rollback evidence | C. REPAIR FIELD-QUALIFICATION BLOCKER | LabQualified | Explicit owner authorization plus real recipe evidence; this does not authorize normal-mode repair. |
| R4 approved-package repair and R5 recovery | D. ACCEPTABLE RC LIMITATION | Not Qualified | No approved newer package or independently verified rollback package exists; normal Doctor remains read-only. |
| Unsigned application binaries | D. ACCEPTABLE RC LIMITATION | NotSigned | No signing capability has been evidenced or invented; any release-policy decision must treat this explicitly. |
| Additional broad external tester coverage after an explicit RC policy decision | E. POST-RC QUALIFICATION | Deferred Post-RC only if governed | Do not silently reclassify the current external matrix as post-RC. |

| Item or remaining gap | Classification | Current safe restriction |
|---|---|---|
| Owner-machine Safe-to-Share bundle inspection | Qualified, narrow | The exact owner artifact passed its stated structure and scoped privacy checks; this does not remove the other privacy, platform, or release gates. |
| Clean/healthy/partial HidHide and Windows 10/alternate-Windows-11 evidence | RELEASE BLOCKER | Fixture results remain FixtureQualified; no cross-machine claim. |
| Native DPI, accessibility, five-theme, and full practical HOTAS review | RELEASE BLOCKER | Do not substitute offscreen/QML fixtures for owner acceptance. |
| User-visible performance, low-resource/write behavior, and broad hardware matrix | RELEASE BLOCKER | Process-level CPU/contention data exists, but no first-visible/native-interaction budget, low-resource test, or broad hardware proof is recorded. |
| Installer, update atomicity, and uninstall | RELEASE BLOCKER | No release decision until paired-component and uninstall safety are exercised sufficiently. |
| External tester run | RELEASE BLOCKER | May become Deferred Post-RC only through an explicit governing release decision. |
| R1–R3 live/UAC/reboot evidence | FIELD-REPAIR BLOCKER | Remain LabQualified and unavailable in normal production mode. |
| ARM64 repair, R4 package, R5 rollback, and app-binary signing | RELEASE-CANDIDATE LIMITATION | No repair promotion or invented signing; kernel/package signature validation remains required. |

The owner-machine diagnosis itself is not a repair authorization. No unresolved
code safety defect was identified by the focused tests, but missing evidence is
not treated as a pass.

# V2.6.9 release qualification

## Scope

This record qualifies the V2.6.9 release-preparation source containing the
approved evidence-resolved DirectInput acquisition implementation and its
focused version/catalog/documentation update. The implementation baseline is
`6fdc7da71935e8da7d5ccccb59b2790c71e3420a`; the release-preparation commit
containing this record supplies the V2.6.9 product identity.

## Local release build and tests

- A clean Release build was configured at `C:\hotas-builds\v269-release-prep`
  with `BUILD_TESTING=ON` and `HOTAS_BUILD_PERFORMANCE_BENCHMARK=ON`.
- The complete configured CTest suite passed: **18/18** in 85.94 seconds. The
  benchmark is the additional eighteenth test in this performance-enabled
  configuration; the standard qualified suite has 17 non-benchmark tests.
- `scripts/sync-documentation.ps1 -Check` passed with `HOTAS_VERSION = 2.6.9`
  and the newest version-catalog entry set to `2.6.9`.

## Acquisition and performance evidence

- Mapping-core fixtures cover one-event and same-value non-verification,
  repeated distinct-value verification, ambiguity, conflicting-source restart,
  T.Flight X to `lY`, T.Flight Y to `lX`, Saitek Rz to `lRz`, signature-bound
  reconnect reuse, stale-signature rejection, and persisted source evidence.
- Direct clean-build benchmark, verified cross-field/one-rig/monitor-closed:
  9,603,841.537 reports/s, 0.085 microseconds average, 0.100 microseconds p95,
  0.100 microseconds p99, and 0 allocations/report.
- This synthetic benchmark measures post-poll fixed-field mapping work only;
  it does not claim DirectInput driver or vJoy-call timing.

## Hardware evidence boundary

Earlier physical evidence established the contradiction shapes represented by
the permanent fixtures: T.Flight canonical X to `lY`, canonical Y to `lX`, and
Saitek canonical Rz to `lRz` despite `DIJOFS_Z`. A fresh exact-build V2.6.9
T.Flight physical acceptance run was not performed because the controller was
unavailable for this release preparation.

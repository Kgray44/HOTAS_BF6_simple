# HOTAS BF6 v1.4 Curve Studio engineering report

Status: source repair compiled and published; live-hardware acceptance remains
open because the physical HOTAS is not currently enumerated by Windows.

## Correctness and configuration

- J is a continuous one-sided progressive curve: `u^p`, where
  `p = 1 + 1.80s`. Centered J remaps `[-1,1]` through `[0,1]` and back. Its
  intentional non-neutral virtual output at physical neutral is surfaced by
  Curve Health and Signal Path.
- Centered S is the required mirrored progressive response:
  `sign(x) * |x|^p`. Unipolar S uses a strength-blended smoothstep that keeps
  `0`, `50%`, and `100%` fixed.
- Response Strength is continuous for every family. Advanced, Personal, and
  Custom use `F(x,s)=(1-s)x+sA(x)`; all resulting shapes compile into the
  immutable 4097-sample LUT before the worker accepts configuration.
- Schema 7 migrates pre-amendment Advanced/Personal/point curves to `100%`
  strength, preserving their former full-response behavior.
- The axis reset defect was the worker/UI fallback which rewrote the selected
  axis when a DirectInput object was unavailable. Selection is now durable
  editor state. Personal selection now enters Personal mode, selects a
  compatible saved preset when available, or preserves an unsaved editable
  Personal response when the library is empty.

## Curve Studio UI

- The selector header uses graphite controls, stable geometry, current
  profile/axis context, a universal strength slider, and all six families:
  Linear, J-Curve, S-Curve, Advanced, Personal, and Custom.
- The graph is a recessed avionics-style instrument with a dark canvas,
  bounded grid, cursor inspection, preview chip, wheel zoom, middle-drag pan,
  and empty-space left-drag pan. Point dragging remains reserved for points.
- Point Editing is a dedicated card with exact Input/Output numeric controls,
  symmetry, interpolation, density, snapping, add/remove, and locking.
  Point and empty-graph right-click menus expose only valid contextual actions.
- View/History groups Frame Curve, Reset Display, Undo, and Redo. Reset
  Display clears only viewport/cursor/comparison/preview state; it never
  changes the active response definition.
- Response Characteristics and Signal Path are permanent cards. Every dashed
  overlay has an owner: comparison, preview, or optional effective response.

## Presets and research

`docs/curve-research.md` records corrected equations, source links, evaluator
rules, universal strength semantics, and all 15 Advanced presets. The five
new `Shooter / Flight Derived` definitions are Shooter Dynamic-Inspired, ADS
Precision-Inspired, Dual-Zone Turn-Inspired, Aircraft Gun Tracking, and
Aircraft Maneuver Progressive. They are labeled as derived approximations,
not proprietary game-exact curves.

## Performance implementation and evidence

- The report path reads only a precompiled immutable LUT. Configuration
  compilation was moved out of the mapping worker and installed as a prepared
  runtime mapping at the configuration boundary.
- The dedicated mapper worker starts at Qt `HighPriority`, deliberately below
  time-critical priority, so a busy Curve Studio yields to physical report
  handling without starving normal rendering or system work.
- The worker records a fixed rolling window of 2,048 completed physical-report
  latencies. A GUI-side timer snapshots it for p95/p99 at most four times per
  second; the report loop only stores one already-measured latency sample and
  never allocates, sorts, or analyses it.
- The benchmark exercises production axis transform, physical monitor, button
  routing, atomics, change detection, and LUT evaluation. It covers Linear,
  high-strength J, high-strength S, Advanced, Shooter/Flight-derived,
  Personal, and 25-point Custom under idle and UI-model stress. It deliberately
  excludes DirectInput and vJoy driver calls when no real device is attached.
- The worker remains change-driven for vJoy outputs: identical stationary
  reports produce no additional axis/button write decisions after initial
  publication.

### Synthetic hot-path evidence before live percentile telemetry

These release-build, report-to-output-decision measurements were captured on
this 16-logical-CPU machine before the later fixed-slot live-percentile
telemetry addition. They use the production 4097-sample LUT and production
transform/button/change-detection code, but deliberately do **not** claim a
DirectInput report cadence, driver-call latency, or vJoy throughput. The
current benchmark source includes the fixed-slot telemetry store, but the
newly linked benchmark is blocked from execution by Windows Application
Control; the required physical/current matrix remains open.

| Condition | Curve | Synthetic reports/sec | Typical µs | p95 µs | p99 µs | Worst µs |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Idle, all CPUs | Linear | 4,302,283 | 0.2 | 0.3 | 0.4 | 85.0 |
| Idle, all CPUs | J-Curve | 4,021,366 | 0.2 | 0.3 | 0.4 | 107.2 |
| Idle, all CPUs | S-Curve | 4,022,122 | 0.2 | 0.3 | 0.4 | 98.8 |
| Idle, all CPUs | Advanced: Precision Tracking | 3,941,174 | 0.2 | 0.3 | 0.5 | 108.8 |
| Idle, all CPUs | Custom: 25 point | 3,991,263 | 0.2 | 0.3 | 0.4 | 89.6 |
| Active UI-model stress, all CPUs | Linear | 3,823,858 | 0.2 | 0.4 | 0.5 | 133.8 |
| Active UI-model stress, all CPUs | J-Curve | 3,654,570 | 0.2 | 0.4 | 0.4 | 107.6 |
| Active UI-model stress, all CPUs | S-Curve | 4,031,080 | 0.2 | 0.3 | 0.4 | 113.0 |
| Active UI-model stress, all CPUs | Advanced: Precision Tracking | 3,834,709 | 0.2 | 0.4 | 0.5 | 314.0 |
| Active UI-model stress, all CPUs | Custom: 25 point | 4,268,752 | 0.2 | 0.3 | 0.4 | 22.0 |

All cases counted zero heap allocations in the measured report loop. Moving
input made about 2.12 million vJoy-axis/button write decisions in 400,000
synthetic reports; after the initial stationary publication, repeated reports
made `0.000` additional writes per report. A single-logical-CPU affinity run
kept p95/p99 at or below `0.4 µs`, but an intentionally continuous UI-model
load produced 22–40 ms scheduler outliers and 2.0–3.2 million synthetic
reports/sec. That is a constrained-CPU scheduler warning, not an acceptable
substitute for the real-device latency acceptance.

Live results required for v1.4 release acceptance are currently **unverified**:
physical reports/sec, end-to-end latency percentiles, worst observed latency,
and vJoy writes/sec while controls move. HidHide’s own inventory currently
reports every stored `VID_044F&PID_B68D` HOTAS instance as not present. vJoy
Device 1 is present. Fresh standalone locally built executables are blocked
from execution by Windows Application Control, although the trusted core-test
runner can execute the embedded benchmark. No number is fabricated in place
of these missing hardware measurements.

## Validation and publication

- Release configuration and builds of `HOTASMapper`, `mapping_core_tests`,
  `mapping_hot_path_benchmark`, and `curve_research_report` completed with
  MSVC 19.44 and Qt 6.8.3.
- QML lint completed without syntax errors; release QML compilation completed.
- The final rebuilt focused core suite passed with the bundled Qt runtime
  (`mapping_core_tests_exit=0`). `curve_research_report` is compiled but its
  standalone executable is blocked by Windows Application Control.
- Branch: `main`
- Published source commit: `dbd065d` (`feat: repair v1.4 curve studio acceptance`)
- Push: successful to `origin/main`

## Remaining acceptance blocker

Reconnect/enumerate the physical HOTAS and permit the freshly built mapper and
benchmark executable under the existing Windows Application Control policy.
Then run the specified physical mapping matrix and UI-stress measurement before
cutting a v1.4.1 release tag. No v1.5 work has begun.
